<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Ejs</title>
</head>

<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10">
            <form action="/create" method="post">
                <input class="block w-full flex-wrap outline-none rounded-md py-2 bg-white " type="text"
                    placeholder="Title goes here  " name="title">

                <textarea class="block w-full outline-none rounded-md  py-2 mt-2  resize-none bg-white " name="details"
                    id="" placeholder="Write your task details here"></textarea>

                <input class="mt-2 bg-blue-500 px-5 py-2 rounded-md" type="submit" value="create task ">
            </form>
        </div>

        <div class="tasks flex gap-3  p-10 ">
            <% if(files.length>0){%>

                <% files.forEach(function(val){%>
                    <div class="task  w-72 px-3 py-4 bg-white  rounded-md ">
                        <h1 class="text-3xl tracking-tighter">
                            <%= val %>
                        </h1>
                        <div class="flex w-full justify-between items-center mt-3 ">
                            <a class="text-blue-600 inline-block  " href="/file/<%= val %>">Read More</a>
                            <a class="text-blue-600 inline-block  " href="/edit/<%= val%>">Edit FileName</a>
                        </div>
                    </div>
                    <%})%>
                        <%} else{%>
                            <h3 class="text-zinc-500"> No task yet</h3>
                            <%}%>
        </div>
    </div>
</body>

</html>